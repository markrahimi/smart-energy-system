services:
  - type: web
    name: smart-energy-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: SERVER_PORT
        value: 8080
